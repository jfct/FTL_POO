#pragma once
#include "main.h"

nave::nave()
{
	setEscudo(100);
}

nave::~nave(){}

sala nave::getSala(int numero){
	vector<sala *>::const_iterator i;
	for (i = vectorSala.begin(); i != vectorSala.end(); ++i){
		if ((*i)->)
	}
	return;
}